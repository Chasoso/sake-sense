# Infrastructure boundary

This directory is reserved for adapters to external APIs or storage. The local shell currently has no infrastructure dependency; future adapters should expose explicit interfaces so domain logic remains browser- and service-independent.
